<Window x:Class="RalseiWarehouse_v2.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ralsei Warehouse - Login" Height="320" Width="380"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Ralsei Warehouse" FontSize="22" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <Label Grid.Row="1" Content="Username"/>
        <TextBox Grid.Row="1" x:Name="txtUserName" Margin="90,2,0,2" Height="24"
                 VerticalContentAlignment="Center"/>

        <Label Grid.Row="2" Content="Password"/>
        <PasswordBox Grid.Row="2" x:Name="txtPassword" Margin="90,2,0,2" Height="24"
                     VerticalContentAlignment="Center" KeyDown="txtPassword_KeyDown"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,24,0,0">
            <Button x:Name="btnLogin" Content="Login" Width="90" Height="28"
                    Margin="0,0,12,0" IsDefault="True" Click="btnLogin_Click"/>
            <Button x:Name="btnCancel" Content="Cancel" Width="90" Height="28"
                    IsCancel="True" Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
